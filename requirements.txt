# 后端依赖（模型推理由 Ollama 完成）
fastapi>=0.110
uvicorn[standard]>=0.29
requests>=2.31
pillow>=10.0

# 语音输入（离线唤醒词「西派西派」+ 流式识别，无需联网）
numpy>=1.24
sounddevice>=0.4
sherpa-onnx>=1.10

# 文生图 / 图片微改（Stable Diffusion，diffusers）
diffusers>=0.30
accelerate>=0.30
torch>=2.2

# 图片高清放大（RealESRGAN 超分）。用 spandrel 加载权重，
# 避免 basicsr/realesrgan 在 Python 3.13+ 上构建失败的问题。
spandrel>=0.4

# 打包应用
pyinstaller>=6.0